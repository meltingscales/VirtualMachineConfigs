server:
  port: 8080

spring:
  application:
    name: customer
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: '${DB_USER}'
    password: '${DB_PASS}'
  sql:
    init:
      platform: postgres
